import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Tech Health Appendix Generator - Ego Eimi Challenge",
  description: "A dynamic pitch deck appendix generator that audits your codebase to build investor confidence.",
  keywords: ["tech health", "code audit", "github analysis", "pitch deck", "startup", "investor ready", "technical debt"],
  authors: [{ name: "Ego Eimi Challenge" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
